{
  "name": "sync-sheets-on-pr-merge",
  "version": "1.0.0",
  "description": "GitHub Action to sync files changed under a folder to a Google Docs when a PR is merged.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.0.0",
    "googleapis": "^121.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
